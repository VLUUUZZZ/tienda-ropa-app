rules_version = '2';

// The app signs in anonymously, so only installs of the app (not random
// requests to the project's API) can read or write the catalog.
service cloud.firestore {
  match /databases/{database}/documents {
    match /prendas/{prendaId} {
      allow read, write: if request.auth != null;
    }
  }
}
